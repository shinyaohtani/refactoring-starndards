# リファクタリング規準書 (JavaScript)

## 1. 基本方針

- JavaScriptなどのコードをスマートかつ読みやすい形にリファクタリングすることを目的とする。
- コード全体の構造を見ただけで流れが把握できるように整理する。

## 2. リファクタリング手順

- コード全体の構成を理解したうえで、
- 必要に応じてクラス分割、責務の明確化を行い、
- 処理の簡素化や統合を実施してスマートな構成に変換し、
- 代替の全コードを提示し、コードのフローや設計意図を簡単に説明し、
- 曖昧な点がある場合はに確認しつつ、基本的には意図を汲み取り進めてください。
- 全ての指示に合致しているか確認し、合致していない部分が見つかったら最初からやり直してください。
- コード実行結果が変更されてしまう場合、リファクタリング失敗であるため、やり直してください。

## 3. モノ指向クラス設計

- とにかくオブジェクト指向を基礎概念として扱い、主体をクラスにして、少しでも動作を示唆するものはクラスにしないでください。
- 動詞や"-er"、"-or"、動詞にもなり得る言葉で終わるような名称は動作を示唆しているためクラスとしては適切ではありません。例: MailFormatterやMailValidationなどはNGです。
- クラスにするものはassetやresult、structureなどモノに特化し、runnerやtranslator, matcher, exporterなど機能をクラスにはしないでください。
- とにかくオブジェクト指向を基礎概念として扱い、メソッド名は「オブジェクトが何をするか、またはオブジェクトの何か」を表す視点で命名してください。
- get_は私の命名規約に合致しないので削除し、例えばXXXAuthクラスのurlにアクセスする場合はurlプロパティ（getter）のように命名してください。
- クラスが多いことは問題ではありません。
- クラス名を見てそのクラスが何をするクラスなのか、全体はどのような流れなのかが理解できるようにしてください。
- すべてのメソッドはクラスに含めてください。クラスの外でメソッドを定義しないでください。

## 4. メソッド・クラス構造制約

- クラスにはメソッドが4個か5個になるようにしてください。 3個では少なすぎます。6個では多すぎます。
- メソッドは必ず25行以下に短くしてください。26行以上になる場合は2つのメソッドに分割してください。
- privateメソッドとpublicメソッドは`#`プライベートフィールド構文または`_`を接頭辞にして区別してください。
- TypeScriptを使用し、すべて型を明記してください。メソッド引数・返り値・変数など全てです。`| null`や`| undefined`を適切に使用してください。

## 5. 命名規約

- メソッド名や変数名、クラス名は、それが担う機能や役割が明確になるように命名してください。
- 抽象的で意味をなさない*info*、*data*、*manager*、*process*、*asset*という言葉は変数名やクラス名に使わないでください。
- pathはabsolute pathとrelative pathで大きく扱いが異なるので、必ずAbsPathかRelPathを接尾語として入れてください。
- file nameはfname、directory nameはdirnameと一貫して略すことで、fnameやdirnameはパスを含まない純粋なファイル名やディレクトリ名を扱うものとしてください。
- listやdictも意味をなさないことが多いです。複数であることを伝えたいだけなら**"s"**をつけるなどで簡潔に表現してください。
- camelCaseを使い、totalFailuresのように表現してください。クラス名はPascalCaseを使用してください。
- 極力1単語で表現し、どうしても1単語では常に誤解を招く場合のみ、camelCaseで繋いでください。その場合は誤解についての評価結果を考えてください。

## 6. 差分チェック対応

- 挙動がstdout, stderr, file outputを含め1mmも変えないようにしてください。
- コード差分チェックのため、後段で「次の2つのコードは同じ目的のために作られたコードです。挙動がまったく同じになるかどうか分析してください。」が問題なく通るようなリファクタリングを行ってください。

## 7. その他

- ESLint, Prettierに対応してください
- パス文字列操作は文字列結合を使わず、Node.jsの`path`モジュールを使って下さい

## 8. React / Next.js 例外規定

React / Next.js はフレームワークが関数ベースの規約を強制するため、§3「すべてのメソッドをクラスに含める」を字面通り適用するとフレームワークと衝突する。以下を例外として定める。**例外はここに列挙したものに限り、例外の内部ロジックは原則どおりモノ指向で設計すること。**

### 8.1 関数コンポーネント

- React コンポーネントは**関数コンポーネントとして定義してよい**(§3「クラス外で関数を定義しない」の例外)。
- **クラスコンポーネントへの書き換えは禁止**する。hooks が使用できず、React の現行慣習(公式推奨)に反するため。
- コンポーネント名は PascalCase の**名詞**とする(§3のモノ指向は維持)。`renderHeader` のような動詞名コンポーネントは禁止。`Header` とすること。
- 子コンポーネントは SwiftUI 子 View と同様、**1関数でも単一責務なら適切**とする。責務の異なるコンポーネントを統合してはならない。統合できるのは「同じ責務の散らばり」のみ。

理由:
- React は関数コンポーネント + hooks を前提に設計されており、クラスコンポーネントは事実上の非推奨API
- コンポーネントはフレームワークが管理する「UIのモノ(名詞)」であり、モノ指向の精神には合致する

### 8.2 フック (hooks)

- `useState` / `useEffect` 等の組み込みフック、および `useXxx` 形式のカスタムフックは、**モジュールスコープの関数として定義してよい**(§3の例外)。
- カスタムフック名は React の規約上 `use` プレフィックスが**必須**であり、§3「動詞で始まる名前の禁止」および §5「get禁止」の**適用外**とする。
- ただし `use` に続く部分は名詞とし、フックが返す「モノ」を表すこと。
    - 例: `useDraftFiles`(添付ファイル群を返す)✓ / `useHandleClick` ✗ → `useClickState` 等に

### 8.3 Next.js 予約シグネチャ(カウント除外・クラス化禁止)

Next.js が名前とシグネチャを固定している以下のエクスポートは、Swift 規準書の「Apple定義プロトコル準拠メソッドの除外」に相当するものとして、メソッド数カウントから**除外**し、モジュールスコープ関数のまま維持する。

- Route Handler: `export async function GET / POST / PUT / DELETE ...`(route.js)
- ページ・レイアウト: `page.jsx` / `layout.jsx` の `export default function`
- メタデータ: `export const metadata`, `export async function generateMetadata`
- その他: `middleware`, `generateStaticParams`, `error.jsx` / `loading.jsx` の default export

理由:
- 名前・エクスポート形式が Next.js 固定であり変更不可能
- クラスのメソッドとして定義するとフレームワークが認識せず動作しない

ただし、Swift の型メソッド例外と同様、**予約シグネチャは薄い入口に留める**こと。本体ロジックを route.js に直接書かず、`lib/` 配下の責務を持つモノ(クラスまたは §8.5 のモジュール)へ委譲すること。

### 8.4 コンポーネントの行数・メソッド数の扱い

- コンポーネント関数が返す **JSX ブロックは §4 の25行制限のカウント対象外**とする(SwiftUI `body` と同じ扱い。JSXは宣言的マークアップであり手続きロジックではない)。
- ただし JSX 以外のロジック部分(state定義、ハンドラ、変換処理)は**合算25行以下**を維持すること。超える場合はカスタムフックまたは子コンポーネントへ分割する。
- コンポーネント内で定義するイベントハンドラ等の内部関数は「メソッド」相当としてカウントし、**6個以上は責務過多の兆候**とする。カスタムフック(状態+ロジック)への抽出、または子コンポーネント分割を検討すること。

```jsx
// 推奨パターン (SwiftUI の @Observable class 分離に相当)
// hooks/useConsultation.js — 状態 + ロジック (4-5関数相当)
export function useConsultation() {
  const [screen, setScreen] = useState('entry');
  const [draft, setDraft] = useState('');
  async function analyze() { /* ... */ }
  async function consult() { /* ... */ }
  return { screen, draft, setDraft, analyze, consult };
}

// app/edit/page.jsx — 純粋なUI組立のみ
export default function EditPage() {
  const c = useConsultation();
  return <main>{/* JSXは行数カウント対象外 */}</main>;
}
```

### 8.5 モジュールスコープの純関数・定数

- React エコシステムでは **ES モジュールがカプセル化の単位**である。export しないモジュールスコープ関数は、クラスの private メソッド相当として許容する(条件: 純関数であり、そのモジュールの責務に属すること)。
- ただし雑多な `utils.js` / `helpers.js` の作成は**禁止**する。モジュール自体を1つのモノ(単一責務)として設計し、ファイル名も名詞にすること。
- `export const` の共有定数は、真に不変な値に限定する(Swift の `static let` 相当)。可変な共有状態をモジュールスコープに置いてはならない。

### 8.6 挙動完全一致との関係 (§6 の具体化)

- `'use client'` / Server Component の境界を変更することは、実行環境(サーバー/クライアント)・バンドルサイズ・hydration 挙動を変えるため、**§6違反として扱う**。リファクタリングでは境界を維持すること。
- hooks はトップレベル呼び出しの規則(Rules of Hooks)があるため、条件分岐内への移動・ループ内への移動は挙動変更であり禁止する。
- `useEffect` の依存配列の変更は実行タイミングを変えるため、§6違反として扱う。

### 8.7 例外を使ってはいけないケース

- コンポーネントでもフックでもない一般ロジック(APIクライアント、ドメイン処理、ファイル変換等)を「Reactだから」と関数の羅列で書くこと。これらは原則(§3)どおりモノ指向のクラスとして設計すること。
- 「クラス化を避けたい」という動機で、本来モノであるべきもの(例: 相談チケット、判定結果)をカスタムフックに押し込むこと。フックは「Reactの状態に接続するモノ」に限る。
