# AI 向けリファクタリング規準書ガイド

このファイルは [`refactoring-standard-swift.md`](./refactoring-standard-swift.md) を初めて適用する生成 AI 向けの解説です。規準書を字面通りに読むと **やりがちな失敗** があるため、実際にこのプロジェクトで発生した失敗を踏まえて読み方を示します。

規準書を読む前にまず本ファイルを読んでください。

---

## 1. 規準書の読み方（誤読しやすい順序）

### 最初に読むべきは §3「モノ指向型設計」

§4「メソッド・構造制約」（4-5 メソッドルール）から読むと、**「メソッド数を減らすこと」が目的化**します。これは典型的な失敗です。

正しい読み順:

1. **§3 モノ指向型設計** ← 最重要。設計の根本思想
2. **§5 命名規約 / §9 簡潔な命名** ← 名詞型・短い名前の文化
3. **§6 差分チェック対応** ← 挙動を絶対変えない
4. **§4 メソッド・構造制約** ← 設計の妥当性チェックとして使う
5. その他

§4 は「結果として 4-5 メソッドに収まる設計が良い」という診断指標であり、**達成すべき目標ではありません**。

---

## 2. モノ指向の本質

### モノ = 名詞 = データを持つ実体

型は「**何を表しているか**」を名詞で答えられないといけません。

#### 良い型名（モノ）

- `Trip`, `PhotoItem`, `ClusterSpot`, `SubscriptionStore`, `LocalizedDate`
- 「これは旅です」「これは写真項目です」「これはローカライズされた日付です」と説明できる

#### 悪い型名（行為・機能）

- `MailFormatter`, `Manager`, `Translator`, `Runner`, `Handler`, `Builder`
- 「メールをフォーマットする**こと**」は行為であり、モノではない

#### 微妙な例（注意が必要）

このプロジェクトで実際に出た例:

| ❌ NG | ✅ OK | 理由 |
|---|---|---|
| `MapBorrow` | `MapLease` / 統合 | "Borrow" は動詞 |
| `BalloonHit` | `BalloonProbe` / 統合 | "Hit" は動詞 |
| `CreditExhaust` | `ExhaustText` / 統合 | "Exhaust" は動詞 |
| `MapTap` | （タップは名詞でもあるので OK） | 状態を持つなら可 |

### 命名チェック

型を作る前に「`〇〇は〜〜だ`（〇〇 is a 〜〜）」と説明文を作る。

- `LocalizedDate` is a localized date. ✓
- `PurchaseErrorText` is a text representing a purchase error. ✓
- `MapBorrow` is a borrowing of a map? **不自然** → リネームか統合を検討

---

## 3. やってはいけない（私が実際にやって叱られた失敗）

### 失敗 1: メソッド数を減らすために型を量産

「16 メソッドある MapRelay を 4 メソッドにしたい」と思って:
- MapBorrow (2 メソッド)
- BalloonHit (3 メソッド)
- MapEvents (3 メソッド)
- MapTap (3 メソッド)
- MapArrows (5 メソッド)
- MapRelay (4 メソッド)

→ ユーザーに **「型を分割するという考え方でメソッド数を減らそうとすると大混乱します。その方法は早々に止めてください」** と叱られた。

**何が悪かったか:**
- 動機が「数を減らす」になっていた
- 各型の責務が薄く、1-3 メソッドの型ばかり生まれた
- それぞれ命名も微妙（Borrow, Hit など動詞混じり）
- 結果として **読み手が型構造を辿るのに型をまたぐ回数が増え、可読性が落ちた**

**正しいアプローチ:**
- まず「このドメインにどんなモノが存在するか」を全体設計として決める
- 自然と 4-5 メソッドに落ち着く設計を選ぶ
- メソッド数は **設計の妥当性を検証する診断指標** であって、達成目標ではない

### 失敗 2: 設計せずファイル単位で着手

「TripSheet.swift を見たら 13 メソッドある、減らそう」とファイル単位で着手すると、ドメイン全体での責務分担が崩れます。

**正しい順序:**

1. ドメイン全体（例: Subscription）の **モノ一覧** を紙の上で設計
   - SubscriptionStore（StoreKit 連携）
   - SubscriptionReceipt（Tx 解釈）
   - AccountMenu（画面）
   - PlanCard（プラン表示カード）
   - …
2. 各モノが持つべき責務とメソッドを書き出す
3. 既存コードを読み、設計に合わせて再配置
4. その結果として 4-5 メソッドに収まっているか検証

### 失敗 3: SwiftUI Views で過剰分割

SwiftUI View は `body` だけの 1 メソッド型が自然です。1 メソッドだから過小だと判断して統合しようとすると、親の `body` が肥大化します。

**ルール:**
- 子 View（`UndoBanner`, `TripListToolbar` 等）は 1 メソッドでも単一責務なら OK
- ただし「UndoBanner と TripListToolbar を 1 つに統合」のように **責務を混ぜる統合は禁止**
- 統合できるのは「同じ責務の散らばり」のみ

### 失敗 4: 挙動の微妙な変化を見逃す

リファクタ中に「より正しい挙動」に変えてしまうと、規準書 §6（挙動を完全一致させる）に違反します。

実際に発生した例:

```swift
// Original (main):
isActive = !expired(tx)  // revocationDate を見ない

// 私のリファクタ:
isActive = tx.revocationDate == nil && exp >= Date()  // ← revocationDate もチェック
```

「より正しい」が「同じ挙動」ではなくなった。

**正しい対処:**
1. リファクタでは **挙動を完全一致** させる
2. リファクタ中に発見した既存バグは **別 issue として起票**
3. 修正は別 PR で行う

### 失敗 5: 行数 25 行制限をリファクタ後に忘れる

メソッド数を整えると安心して、各メソッドの **行数 25 行** チェックを忘れがちです。両方同時に守る必要があります。

監査スクリプト例:

```bash
# 25 行超のメソッド/var を検出
awk '
/^[[:space:]]*(private |fileprivate |internal |public )?(@.*[[:space:]]+)?(var |func )/ {
  if (in_block && lines > 25) print prev_file":"prev_line": "lines" lines"
  in_block = 1; lines = 0; prev_line = NR; prev_file = FILENAME
  brace_count = gsub(/\{/, "{") - gsub(/\}/, "}")
  next
}
in_block {
  lines++
  brace_count += gsub(/\{/, "{") - gsub(/\}/, "}")
  if (brace_count <= 0) in_block = 0
}
' **/*.swift
```

---

## 4. リファクタリングの正しい進め方

### 手順

1. **対象ドメインを明確化** （例: 「Subscription ドメインのみ」）
2. **既存コードを読み、現状の責務を整理**
   - どのファイルにどんな処理があるか
   - 重複・散らばりはないか
3. **モノとして再設計**（紙 / Markdown 上で）
   - 各モノの名前（名詞、2 単語以内）
   - 各モノの責務（1 文で説明できるか）
   - 各モノが持つメソッド一覧
4. **設計をユーザーに共有・合意**
5. **実装**
   - 1 モノずつ作成・移行
   - ビルド成功を都度確認
6. **挙動完全一致を監査**
   - `git diff` で全変更を確認
   - 副作用・UI 表示・エラー文言など細部まで確認
7. **PR 作成 / レビュー / マージ**

### 設計提示の例（このプロジェクトの Subscription ドメイン）

```text
主要モノ (各 4-5 メソッド):
- SubscriptionStore: StoreKit 操作とサブスク状態の単一窓口 (5)
- AccountMenu: アカウント画面のリフレッシュライフサイクル + 構成 (5)
- PlanCard: プランバッジ + クレジット表示カード (4)
- PremiumOffer: アップグレード提案カード (5)

ドメインモデル:
- SubscriptionReceipt: Transaction 解釈 (3, 単一責務)

サブビュー (単一責務、1-3 メソッド):
- AccountManageBox, AccountSupportBox, AccountFooter
- PlanBar, PremiumFeatures, PurchaseErrorText

共通ユーティリティ:
- LocalizedDate: ユーザ言語設定での日付整形
- Bundle.appVersionLabel: アプリ版表示 (extension)
```

このように **責務を文章で説明できる粒度** でモノを並べてから着手する。

---

## 5. 単一責務型（1-3 メソッド）の判断基準

§4 §単一責務型の最小メソッド数 で例外が認められていますが、安易に使ってはいけません。

### 例外を使ってよいケース

- **Apple プロトコルの実装**だけが責務（`UIContextMenuInteractionDelegate` のみ等）
- **SwiftUI View 子コンポーネント**で `body` のみ
- **値変換の薄いユーティリティ**（`LocalizedDate` 等）

### 例外を使ってはいけないケース

- 「メソッド数を 4-5 に収めるため、親の責務を機械的に切り出した」
- 切り出した型の責務を 1 文で説明できない
- 親と子で同じデータを持ち合っている（データの所在が曖昧）

---

## 6. 規準書のどこを優先するか（衝突時）

複数のルールが衝突した場合の優先順位:

1. **挙動完全一致**（§6）— 最優先。これを破る変更は失敗
2. **モノ指向**（§3）— 設計の根本
3. **メソッド 25 行以下**（§4）— 必須
4. **メソッド 4-5 個**（§4）— 設計の妥当性指標
5. その他のスタイルルール

「§4 を満たすために §3 や §6 を犠牲にする」のは典型的な失敗です。

---

## 7. ユーザーとの対話パターン

### Do: 設計を先に共有

```
「Subscription ドメインのモノを以下のように設計しました:
- SubscriptionStore (5 メソッド)
- SubscriptionReceipt (3, 単一責務)
- ...
この設計で進めてよいですか？」
```

設計に合意してから実装を始める。

### Don't: 着手後に承認を求める

```
「リファクタしました。レビューしてください」  ← 既に動かない型が量産済み
```

設計が間違っていると全てやり直しになります。

### Don't: 細切れ確認

```
「ファイル A を refactor しました、確認してください」
（数分後）「次にファイル B を refactor しました、確認してください」
```

ユーザーから **「途中で確認させるのは止めてください。完璧な状態になってからにしてください」** と注意される。
ドメイン単位で完成させてから 1 度に確認を求める。

---

## 8. このプロジェクト固有の注意

### ビルド

`./gen_build_install.zsh --build-check` を使う。`xcodebuild` / `xcodegen` を直接呼ばない。

### worktree

このプロジェクトは複数の git worktree を使うことがある。`git worktree list` で配置を確認してから作業する。

### CLAUDE.md / メモリ

`CLAUDE.md` と `.claude/projects/<path>/memory/` の指示を必ず読む。重複した指示は出さない。

---

## まとめ

> **メソッド数 4-5 はゴールではなく、良い設計の副産物です。**
>
> モノ指向（名詞型 / 単一責務）で設計したら自然と 4-5 メソッドに収まる、という関係性であり、逆ではありません。

この順序を間違えると、本ガイドの「失敗 1〜5」を必ず再現します。先輩 AI（このセッションの私）も全部やりました。
