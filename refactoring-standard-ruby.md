# リファクタリング規準書 (Ruby)

## 1. 基本方針

- Ruby のコードを、Ruby らしく（Rubyish）、かつ読みやすくスマートな形にリファクタリングすることを目的とする。
- コード全体の構造（クラス・モジュール定義）を見ただけで、データと処理の流れが把握できるように整理する。

## 2. リファクタリング手順

- コード全体の構成を理解したうえで、
- 必要に応じて **Class / Data** への分割、責務の明確化を行い、**振る舞いは原則としてインスタンスメソッドに配置**し、
- 処理の簡素化（`map` / `select` / `reduce` / `each` / ガード節 / 安全ナビゲーション `&.` の活用）や統合を実施してスマートな構成に変換し、
- 代替の全コードを提示し、コードのフローや設計意図を簡単に説明し、
- 曖昧な点がある場合は確認しつつ、基本的には意図を汲み取り進めてください。
- 全ての指示に合致しているか確認し、合致していない部分が見つかったら最初からやり直してください。
- コード実行結果（標準出力・エラー出力・ファイル出力・例外の種類などの副作用を含む）が変更されてしまう場合、リファクタリング失敗であるため、やり直してください。

## 3. モノ指向クラス設計

- オブジェクト指向を基礎概念として扱い、主体をクラスにし、少しでも動作を示唆するものはクラスとして定義しないでください。
- 動詞や `-er` / `-or`、動詞にもなり得る言葉で終わる名称は動作を示唆しているため、クラス名としては不適切です。（例: `MailFormatter` NG → `MailFormat` Good）
- クラスにするものは `Asset` や `Result`、`Structure` など「モノ（名詞）」に特化し、`Runner` / `Translator` / `Matcher` / `Exporter` など機能をクラス名にしないでください。
- メソッド名は「そのオブジェクトが何をするか（Action）」または「そのオブジェクトの何か（Property）」を表す視点で命名してください。
  - 真偽値を返すメソッドは末尾に `?`、破壊的・副作用の強いメソッドは末尾に `!` を付けてください。
- `get_` プレフィックスは禁止です。値を返すだけなら `attr_reader` か名詞のメソッドにし、引数が必要なときのみ動詞から始めてください。
  - 例: `XxxAuth` の URL → `attr_reader :url` または `def url` （`get_url` は不可）
- 不変な値オブジェクトは `Data.define`（Ruby 3.2+）を第一候補にし、可変な実体が必要なときのみ `Class` にしてください。`Struct` の可変利用は避けてください。
- クラス・モジュール定義が多くなることは問題ではありません。名前を見て「何を表すモノなのか」「全体はどんな流れか」が分かるようにしてください。
- すべてのメソッドはクラスに含めてください。トップレベル（`Object` / `main`）やモジュールの特異メソッドとしてメソッドを定義しないでください。
- `def self.foo` などのクラスメソッド（実質 static method）は**原則禁止**です。責務を持つインスタンスへ委譲してください。モノ指向で責務を検討すれば、自然とクラスメソッドを必要としない編成になります。
  - 例外は、**ライブラリ・フレームワークがクラスメソッドを要求する場合**、または **`freeze` した真に不変な定数公開**のみ。この場合もクラスメソッドは薄い入口に留め、本体ロジックは別の責務を持つモノへ委譲してください。
- `Module` は名前空間（ネームスペース）としてのみ使ってください。`include` / `prepend` / `extend` による振る舞いの共有（Mixin）は禁止です。共有したい振る舞いは、責務を持つ名詞クラスに置き、合成（has-a）または継承で使ってください。
- 実装の多重継承は禁止です。責務の所在が一意でなくなり、合成で常に代替できます。基底クラスは1つまでとし、Ruby では Mixin（上記）がその手段にあたるため同様に禁止します。例外は、ライブラリが多重 include を強制する場合、およびフレームワーク基底クラス1つ + 実装を持たないマーカー的 include の併用のみです。

### 同一責務は集約する

- まったく別の箇所で使われていても、**責務が同じコードは1つのクラスにまとめてください**。特定のドメインに縛られない名詞クラスとして切り出し、両方の箇所でそのクラスを使い回します。
- 責務が同じかどうかは、それぞれの役目を「〜を〜する」と一文で言い表し、その一文が一致するかで見ます。実装の形やメソッド数は判断材料にしません。中身の書き方が違っていても、まとめる際に一方へ揃えます。
- 例: 通信の受信履歴を後で参照する処理と、ユーザ入力の履歴を後で参照する処理は、役目がどちらも「履歴を保持して後で使う」なので `History` に統合します。クラス名は `InputHistory` のようにドメイン名を冠さず、`History` のような一般名詞にします。
- まとめても責務は1つのままなので、メソッド数の目安（§4）には響きません。重複が消えるぶん総数はむしろ減ります。
- いずれ片方だけ別の役目に育ちそうなものは、1つのクラスに押し込めないでください。ただし共通部分を基底クラスとして切り出せるなら、そこは継承で共有します。

## 4. メソッド・構造制約

- 1つのクラスに含まれるメソッドは4〜5個を目安にしてください。3個未満では少なすぎ、6個以上は多すぎます（責務過多の兆候）。
  - `class << self` や `refine`、複数ファイルに分けた `reopen` を含め、同一型のメソッドはすべて合算してカウントしてください。分割しても総数は変わりません。

### カウント対象の定義

- **カウント対象**: インスタンスメソッド（`def`）、クラスメソッド（`def self.` / `class << self` 内の `def`）、複数行のロジックを持つ `define_method`
- **カウント対象外**:
  - `initialize`
  - `attr_reader` / `attr_writer` / `attr_accessor` による1行アクセサ
  - `Data.define` / `Struct.new` が自動生成するアクセサ

### メソッドの長さ・可視性

- メソッドは必ず25行以下にしてください。26行以上になる場合は `private` なヘルパーメソッドに分割してください。
  - ガード節（`return unless ...`）や早期 `raise` を使ってネストを浅くしてください。
- `private` / `protected` を適切に使い、外部に公開する必要のないメソッドは徹底して隠蔽してください。Python の `_` 接頭辞ルールは使わず、`private` キーワードで表現してください。
- 引数が3個以上、または真偽値・意味の近い値が並ぶ場合はキーワード引数にしてください。
- ファイル先頭に `# frozen_string_literal: true` を付けてください。
- 型情報は **RBS** のシグネチャ、または YARD の `@param` / `@return` で明示してください。曖昧な `nil` の受け渡しは避け、ガード節で早期に処理してください。

## 5. 命名規約

- メソッド名・変数名・クラス名は、役割が明確になるように命名してください。
- 抽象的で意味をなさない語（`info`, `data`, `manager`, `process`, `asset`）を変数名・クラス名に使わないでください。文脈を含んだ具体的な名前にしてください。
- パスは文字列ではなく `Pathname` を前提とし、変数名は `absolute_path` / `relative_path` のように明記してください。
- ファイル名は `file_name`、ディレクトリ名は `directory_name` とし、略語（`fname` 等）は使わないでください。
- 配列・ハッシュであることを示す `_list` / `_hash` 接尾辞は禁止です。複数形（`items`, `users`）で表現してください。
- メソッド名・変数名は **snake_case**、クラス・モジュール名は **CamelCase**、定数は **SCREAMING_SNAKE_CASE** にしてください。
- 極力1単語で表現し、誤解を招く場合のみ snake_case で連結してください。クラス名は2単語以内に収めてください。

## 6. 差分チェック対応

- リファクタリング前後で、標準出力・エラー出力・ファイル出力・発生する例外クラス・戻り値の挙動が完全に一致するようにしてください。
- 「次の2つのコードは同じ目的のために作られたコードです。挙動がまったく同じになるかどうか分析してください。」という後段のチェックが問題なく通るリファクタリングにしてください。
- リファクタリング中に発見した既存バグは修正せず、別 issue として起票してください。

## 7. スタイルガイド・Lint 適合

- **RuboCop**（`rubocop-performance` を含む）または **Standard** のいずれかを用い、**違反0件**を維持してください。
- 自動フォーマッタを通した際に大幅な差分が出ない標準的なスタイルにしてください。
  - インデントはスペース2つ。
  - `Metrics/MethodLength`・`Metrics/AbcSize`・`Metrics/ClassLength` を無効化・緩和して規準を回避しないでください。
- パス操作は文字列結合や `File.join` の乱用を避け、`Pathname` と `FileUtils` を使ってください。
- 例外は握りつぶさず、`rescue => e` のような無指定 rescue を避け、捕捉する例外クラスを明示してください。
- メソッド間・論理ブロック間に1行の空行を入れて可読性を確保してください。

## 8. Rails 例外規定

Rails はフレームワークが基底クラスと命名規約を強制するため、§3・§4 を字面通り適用すると衝突する。以下を例外として定める。**例外はここに列挙したものに限り、例外の内部ロジックは原則どおりモノ指向で設計すること。**

### 8.1 フレームワーク基底クラスとアクションメソッド

- `ApplicationController` / `ApplicationRecord` / `ApplicationJob` / `ApplicationMailer` を継承したクラスは、基底クラス変更・PORO 化を**禁止**する（フレームワークが認識しなくなるため）。
- コントローラのアクション（`index`, `show`, `create`, `update`, `destroy` 等）は、シグネチャが Rails 固定であり、Swift 規準書の「Apple 定義プロトコル準拠メソッドの除外」に相当するものとして、**メソッド数カウントから除外**する。
- ただしアクションは**薄い入口**に留め、本体ロジックを直接書かない。`app/models` 配下の名詞 PORO へ委譲すること。

### 8.2 宣言的 DSL はメソッドではない

- `has_many` / `belongs_to` / `validates` / `before_action` / `scope` / `has_one` 等のクラスマクロは**宣言**であり、メソッド数にカウントしない。
- `scope :active, -> { where(archived: false) }` はクラスメソッド禁止規定の例外とする。複雑な条件はスコープ名だけ残し、本体を名詞クラスへ委譲すること。

### 8.3 Concern モジュール

- `ActiveSupport::Concern` による Mixin は原則使わないこと（§3 の Mixin 禁止）。共有ロジックは責務を持つ名詞 PORO に置き、モデルからは合成（委譲）で使うこと。
- gem がインターフェースとして特定の Concern の include を要求する場合に限り例外とし、その Concern は薄い委譲に留め、`Archivable` のような形容詞名にすること。

### 8.4 View ヘルパー

- `app/helpers` のヘルパーは、フレームワークがモジュールのメソッド群を要求するため例外とする。表示整形の薄いロジックに限定し、ドメインロジックを置かないこと。

### 8.5 例外を使ってはいけないケース

- `app/services/foo_creator.rb` のような**動詞名のサービスオブジェクト**を、ロジックの既定の置き場として量産すること。JavaScript 規準書の `utils.js` 禁止、Swift 規準書の機能名型禁止と同じ扱い。ドメイン処理は名詞 PORO（`app/models`）として設計すること。
- コントローラやモデルに、永続化・リクエスト処理を超える業務ロジックを書くこと。名詞 PORO へ抽出すること。
